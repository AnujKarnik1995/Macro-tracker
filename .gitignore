# Gradle
.gradle/
build/
app/build/

# Android Studio / IntelliJ
.idea/
*.iml
.DS_Store
captures/

# Local config (machine-specific — has the SDK path)
local.properties

# Keystore / secrets
*.jks
*.keystore

# Personal data exports used by the offline tests — keep only the synthetic
# fixtures under backend/test/fixtures/. Real Tracker/Summary/FormResponses
# exports dropped in backend/test/ stay out of the repo.
backend/test/*.csv
